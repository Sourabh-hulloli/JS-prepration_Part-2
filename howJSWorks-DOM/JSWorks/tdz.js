// Uncaught ReferenceError:
// Cannot access 'firstName' before initialization

// console.log(firstName);

let firstName = "Sourabh";
console.log(firstName);

// Uncaught ReferenceError:
// lastName is not defined
// console.log(lastName);

// Uncaught ReferenceError: Cannot access
// 'userName' before initialization
// console.log(userName);
// let userName; // only decleration
// console.log(userName);

console.log(typeof fullName); // if the variable is not declared nor initilized in code if we check the typeof the variable it will print undefined.
